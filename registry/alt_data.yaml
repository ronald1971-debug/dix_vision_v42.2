# registry/alt_data.yaml
# Alternative data engine defaults (Wave 2).

version: v1

alt_data:
  news_lookback_hours: 24
  social_lookback_hours: 24
  sentiment:
    news_weight: 0.4
    social_weight: 0.4
    orderflow_weight: 0.2
