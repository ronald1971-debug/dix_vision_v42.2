# DIX VISION v42.2 — Complete pip install list
# Organized by integration tier
# Last updated: 2026-05-08
#
# Usage:
#   pip install -r requirements-tier-s.txt          ← start here
#   pip install -r requirements-tier-a.txt          ← after tier S is integrated
#   pip install -r requirements-tier-b.txt          ← after tier A is integrated
#   pip install -r requirements-tier-c.txt          ← future phases
#
# Or install a single tier all at once:
#   pip install ccxt hftbacktest nautilus_trader ... (see per-tier files below)
#
# NOTES:
#   [NO PIP] = install from source or binary, see notes
#   [OPTIONAL] = only needed if using that specific backend
#   [HEAVY] = large install, install only when needed
# ================================================================


# ================================================================
# TIER S — Install now, one at a time as you integrate each module
# ================================================================

# 1. ccxt → execution_engine/adapters/binance.py
ccxt

# 2. hftbacktest → simulation/slippage_model.py, latency_model.py
hftbacktest

# 3. nautilus_trader → simulation/engine.py, event_replayer.py
nautilus_trader

# 4. qlib → learning_engine/performance_analysis/pnl_attribution.py
# NOTE: install alias is pyqlib not qlib
pyqlib

# 5. firecrawl → sensory/web_autolearn/crawler.py
firecrawl-py

# 6. pydantic-ai → intelligence_engine/cognitive/chat/registry_driven_chat_model.py
pydantic-ai

# 7. river → learning_engine/lanes/self_learning_loop.py
river

# 8. faiss → state/memory_tensor/episodic.py, semantic.py
# CPU version (use faiss-gpu only if GPU available and not in hot path)
faiss-cpu

# 9. feast → state/feature_store.py, dataset_registry.py
feast

# 10. polars → learning_engine analytics, simulation/scoring_engine.py
polars

# 11. cryptofeed → execution_engine/market_data/aggregator.py
cryptofeed

# 12. litellm → intelligence_engine/cognitive AI provider routing
litellm


# ================================================================
# TIER A — Install after Tier S modules are working
# ================================================================

# 13. stable-baselines3 → evolution_engine/sandbox.py, learning_engine/lanes/ral.py
# [extra] installs tensorboard, rich progress bars
stable-baselines3[extra]

# 14. langgraph → intelligence_engine/cognitive/approval_edge.py, debate_round.py
langgraph

# 15. dspy-ai → intelligence_engine/cognitive/chat/registry_driven_chat_model.py
dspy-ai

# 16. opa-python-client → governance_engine/services/trust_engine.py
# NOTE: also requires OPA binary — see [NO PIP] section below
opa-python-client

# 17. prometheus-client → system_engine health monitors, liveness watchdog
prometheus-client

# 18. opentelemetry → system_engine/health_monitors, ui/server.py tracing
opentelemetry-sdk
opentelemetry-instrumentation-fastapi
opentelemetry-exporter-otlp-proto-http

# 19. qdrant-client → state/memory_tensor/semantic.py, profile_store.py
# Use local embedded mode, no server needed initially
qdrant-client

# 20. neo4j → state/knowledge_store.py, archetype_store.py
neo4j

# 21. networkx → core/causal_graph.py, fix dep_graph_valid: False
networkx

# 22. hypothesis → tests/test_replay.py, test_replay_gate.py (property-based tests)
hypothesis

# 23. duckdb → simulation/scoring_engine.py, learning analytics
duckdb

# 24. helius-py → execution_engine/adapters/memecoin/, sensory/trader_intelligence
# NOTE: community package, verify before install
helius

# 25. abides-markets → simulation/adversarial/flash_crash_synth.py, stop_hunter.py
abides-markets

# 26. playwright → sensory/web_autolearn/crawler.py (browser fallback)
playwright
# After install, also run: playwright install chromium


# ================================================================
# TIER B — Install after simulation/ and learning_engine/ are built
# ================================================================

# 27. ray[default] → simulation/parallel_runner.py (offline only, never hot path)
# [HEAVY] ~500MB install
ray[default]

# 28. ray[rllib] → evolution_engine multi-agent RL (install separately after ray[default])
# ray[rllib]  ← uncomment when needed

# 29. evotorch → evolution_engine/genetic/ CMA-ES strategy evolution
evotorch

# 30. deap → evolution_engine/genetic/crossover.py, promotion_engine.py
deap

# 31. finrl → learning_engine/lanes/ral.py, evolution_engine/sandbox.py
# [HEAVY] installs torch, gym, many deps
finrl

# 32. nevergrad → evolution_engine/genetic/mutation_operators.py
nevergrad

# 33. pyautogen → intelligence_engine/agents/adversarial_observer.py, debate_round.py
# NOTE: package name is pyautogen not autogen
pyautogen

# 34. pandas-ta → sensory/behavior_analyzer.py, archetype_lifecycle.py
pandas-ta

# 35. tsfresh → learning_engine/trader_abstraction/extractor.py
tsfresh

# 36. scrapy → sensory/web_autolearn/crawler.py (deep-crawl fallback)
scrapy

# 37. spacy → sensory/ai_filter.py, news_fanout.py NER
spacy
# After install, also run: python -m spacy download en_core_web_sm

# 38. z3-solver → tools/replay_validator.py, tools/enforcement_matrix.py
z3-solver

# 39. backtrader → simulation/backtester.py, event_replayer.py
backtrader

# 40. sentry-sdk → system_engine/anomaly_detector.py error capture
sentry-sdk

# 41. vectorbt → simulation/scoring_engine.py PnL metrics
# [HEAVY] installs numba, plotly, many deps
vectorbt

# 42. jesse → intelligence_engine/strategy_runtime/archetype_lifecycle.py
jesse

# 43. mlflow → evolution_engine/pipeline.py experiment tracking
mlflow


# ================================================================
# TIER C — Future phases, do not install until needed
# ================================================================

# Neuromorphic
# 44. norse → sensory/neuromorphic/indira_signal.py, dyon_anomaly.py
norse
# [HEAVY] requires PyTorch

# 45. bindsnet → sensory/neuromorphic/governance_risk.py
bindsnet
# [HEAVY] requires PyTorch

# 46. brian2 → sensory/neuromorphic/ (prototyping only, convert to Norse for prod)
brian2

# Event streaming
# 47. bytewax → system/async_bus.py future upgrade
bytewax

# 48. kafka-python → system/async_bus.py distributed mode
# also used for Redpanda (Kafka-compatible)
kafka-python

# Federated learning
# 49. flwr → learning_engine/lanes/federated.py
flwr

# 50. fedml → learning_engine/lanes/federated.py (alternative to Flower)
fedml

# 51. openfl → learning_engine/lanes/federated.py (Intel alternative)
openfl

# AI orchestration
# 52. semantic-kernel → intelligence_engine/cognitive/approval_edge.py
semantic-kernel

# 53. metagpt → evolution_engine/critique_loop.py (role-based critique)
metagpt

# Knowledge / vector
# 54. haystack-ai → state/knowledge_store.py RAG pipeline
haystack-ai

# 55. weaviate-client → state/memory_tensor/semantic.py (Qdrant alternative)
weaviate-client

# 56. gqlalchemy → state/knowledge_store.py (Memgraph Cypher interface)
gqlalchemy

# Code intelligence
# 57. tree-sitter + Python grammar → tools/contract_diff.py, evolution_engine/critique_loop.py
tree-sitter
tree-sitter-python

# RL research
# 58. torchrl → learning_engine/lanes/policy_distillation.py
# [HEAVY] requires PyTorch
torchrl

# 59. gym-anytrading → evolution_engine/sandbox.py gym.Env reference
gym-anytrading

# Secrets management
# 60. hvac → core/secrets.py HashiCorp Vault client
hvac

# Model serving (local LLMs)
# 61. vllm → intelligence_engine/cognitive local model fallback
# [HEAVY] requires GPU ideally, large install
# vllm   ← uncomment when ready

# 62. ollama → intelligence_engine/cognitive local model fallback
# Python client only, requires Ollama binary separately
ollama

# Portfolio optimization
# 63. PyPortfolioOpt → intelligence_engine/portfolio/risk_parity.py, correlation_engine.py
PyPortfolioOpt

# 64. zipline-reloaded → simulation/backtester.py (cross-validation reference)
zipline-reloaded

# Time-series databases
# 65. questdb → state/ledger/hot_store.py (optional high-frequency backend)
questdb

# 66. kats → system_engine/hazard_sensors/neuromorphic_detector.py drift detection
kats

# 67. pyarrow → state/ledger/snapshots.py Parquet snapshots
pyarrow

# 68. disruptor → execution_engine/hot_path (LMAX ring buffer Python port, benchmark only)
disruptor

# 69. PyLOB → simulation/adversarial/flash_crash_synth.py lightweight orderbook
PyLOB

# 70. cleanrl → learning_engine/lanes/policy_distillation.py
# NOTE: no pip package — install from source:
# git clone https://github.com/vwxyzjn/cleanrl && pip install -e cleanrl/


# ================================================================
# NO PIP — Binary installs or source-only
# ================================================================

# OPA binary (needed alongside opa-python-client):
#   curl -L -o opa https://openpolicyagent.org/downloads/latest/opa_linux_amd64
#   chmod +x opa && mv opa /usr/local/bin/

# Playwright browsers (after pip install playwright):
#   playwright install chromium

# spaCy model (after pip install spacy):
#   python -m spacy download en_core_web_sm

# Grafana (deploy as Docker container, no pip):
#   docker run -d -p 3000:3000 grafana/grafana

# Prometheus (deploy as binary, no pip):
#   https://prometheus.io/download/

# Ollama binary (needed alongside pip install ollama):
#   curl -fsSL https://ollama.ai/install.sh | sh

# Freqtrade (source-only recommended for production):
#   git clone https://github.com/freqtrade/freqtrade
#   cd freqtrade && ./setup.sh -i

# Hummingbot (source-only, no reliable pip):
#   git clone https://github.com/hummingbot/hummingbot
#   cd hummingbot && ./install

# SpykeTorch (no pip, source only):
#   git clone https://github.com/neuromorphs/SpykeTorch
#   pip install -e SpykeTorch/

# TLA+ (Java toolbox, not a Python package):
#   https://github.com/tlaplus/tlaplus/releases

# Sourcegraph sg CLI (binary):
#   curl -L https://github.com/sourcegraph/sourcegraph/releases/... -o sg

# n8n (Node.js, not Python):
#   npm install -g n8n

# Aeron (Java, architecture reference only):
#   No install needed — read source for design patterns only

# PyO3 / maturin (Rust build tool, for when you write Rust extensions):
#   pip install maturin
#   # then: maturin develop --release in your Rust project


# ================================================================
# QUICK REFERENCE — Single-line install per tier
# ================================================================

# TIER S (run this first):
# pip install ccxt hftbacktest nautilus_trader pyqlib firecrawl-py pydantic-ai river faiss-cpu feast polars cryptofeed litellm

# TIER A (run after Tier S is integrated):
# pip install stable-baselines3[extra] langgraph dspy-ai opa-python-client prometheus-client opentelemetry-sdk opentelemetry-instrumentation-fastapi opentelemetry-exporter-otlp-proto-http qdrant-client neo4j networkx hypothesis duckdb helius abides-markets playwright

# TIER B (run after simulation/ and learning_engine/ are complete):
# pip install ray[default] evotorch deap nevergrad pyautogen pandas-ta tsfresh scrapy spacy z3-solver backtrader sentry-sdk vectorbt jesse mlflow

# TIER C (install one at a time only when that module is being built):
# See individual entries above
