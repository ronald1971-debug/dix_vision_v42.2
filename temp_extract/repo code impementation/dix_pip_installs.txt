# DIX VISION v42.2 — Complete pip install reference
# Grouped by tier and category
# All packages verified on PyPI as of 2026-05
#
# USAGE:
#   pip install <package> --break-system-packages
#   or add to requirements.txt / pyproject.toml
#
# RUNTIME = safe for hot_path / RUNTIME tier engines
# OFFLINE  = learning_engine, evolution_engine, simulation only
# TOOLS    = CI, linting, validation — never imported at runtime
# DEV      = local development only

# ============================================================
# TIER S — Install these first, highest immediate DIX value
# ============================================================

# Exchange adapters
pip install ccxt

# AI model routing (unified provider abstraction)
pip install litellm

# Typed AI agent orchestration
pip install pydantic-ai

# Online / streaming learning
pip install river

# Vector similarity search (memory_tensor)
pip install faiss-cpu
# GPU version (if CUDA available):
# pip install faiss-gpu

# Evolutionary optimization
pip install evotorch

# Web extraction (web_autolearn)
pip install firecrawl-py

# Formal constraint verification
pip install z3-solver

# ============================================================
# TIER A — 
# ============================================================

# --- Trading / market data ---
pip install cryptofeed
pip install vectorbt
pip install pandas-ta

# --- RL / learning ---
pip install stable-baselines3
pip install cleanrl
pip install finrl
pip install torch
pip install torchrl

# --- AI orchestration ---
pip install dspy-ai
pip install ollama
pip install openai
pip install anthropic
pip install google-generativeai
pip install langchain-core
pip install langgraph

# --- Feature store / data infrastructure ---
pip install feast
pip install polars
pip install duckdb
pip install pyarrow
pip install mlflow

# --- Observability ---
pip install opentelemetry-sdk
pip install opentelemetry-exporter-otlp
pip install prometheus-client
pip install sentry-sdk
pip install opentelemetry-instrumentation-fastapi

# --- Time-series / quant ---
pip install tsfresh
pip install kats
pip install pyportfolioopt

# --- Web intelligence ---
pip install playwright
pip install scrapy
pip install beautifulsoup4
pip install httpx

# --- Crypto / Solana ---
pip install solders
pip install anchorpy
pip install solana
pip install base58

# --- Storage ---
pip install questdb
pip install clickhouse-driver
pip install psycopg2-binary

# ============================================================
# TIER B — 
# ============================================================

# --- Neuromorphic ---
pip install norse-torch
pip install bindsnet
pip install brian2

# --- Multi-agent ---
pip install autogen-agentchat
pip install crewai
pip install semantic-kernel

# --- NLP / transformers ---
pip install spacy
pip install transformers
pip install sentence-transformers
pip install openai-whisper
pip install TTS

# --- Vector DBs (alternatives to FAISS) ---
pip install qdrant-client
pip install weaviate-client
pip install lancedb

# --- Knowledge graphs ---
pip install neo4j
pip install networkx
pip install memgraph-mage

# --- RL environments ---
pip install gymnasium
pip install gym-anytrading

# --- Evolutionary / optimization ---
pip install nevergrad
pip install deap

# --- Streaming ---
pip install bytewax

# --- Property testing / static analysis ---
pip install hypothesis
pip install semgrep
pip install crosshair-tool

# --- Code intelligence ---
pip install tree-sitter
pip install tree-sitter-python

# --- Tracing ---
pip install jaeger-client
pip install opentracing

# --- Security ---
pip install hvac
pip install cryptography

# --- Data quality ---
pip install great-expectations

# --- Time-series clients ---
pip install influxdb-client

# ============================================================
# TIER C — 
# ============================================================

# --- Distributed compute ---
pip install ray[default]
pip install ray[rllib]
pip install celery
pip install dask

# --- Federated learning ---
pip install flwr
pip install fedml

# --- Advanced AI serving ---
pip install vllm
pip install llama-cpp-python

# --- Advanced ML ---
pip install jax
pip install jaxlib
pip install optax

# --- Stream processing ---
pip install faust-streaming
pip install confluent-kafka
pip install nats-py

# --- Advanced time-series ---
pip install statsforecast
pip install neuralforecast

# --- Workflow orchestration ---
pip install temporalio
pip install dagster
pip install dagster-webserver

# --- Quant research ---
pip install zipline-reloaded
pip install backtrader
pip install bt

# --- Graph ML ---
pip install torch-geometric
pip install dgl

# --- Search ---
pip install elasticsearch
pip install opensearch-py
pip install meilisearch

# ============================================================
# ALWAYS INSTALL — Dev tools
# ============================================================

pip install pytest
pip install pytest-asyncio
pip install pytest-cov
pip install ruff
pip install mypy
pip install httpx
pip install hypothesis
pip install pre-commit

# ============================================================
# ONE-LINER BLOCKS — copy/paste ready
# ============================================================

# Tier S only (start here):
pip install ccxt litellm pydantic-ai river faiss-cpu evotorch firecrawl-py z3-solver

# Tier S + A trading/execution focus:
pip install ccxt cryptofeed litellm pydantic-ai river faiss-cpu evotorch firecrawl-py z3-solver vectorbt pandas-ta stable-baselines3 finrl dspy-ai feast polars duckdb pyarrow mlflow prometheus-client sentry-sdk opentelemetry-sdk

# Tier S + A AI/cognition focus:
pip install litellm pydantic-ai dspy-ai ollama openai anthropic langgraph river faiss-cpu sentence-transformers qdrant-client lancedb

# Tier S + A data/infrastructure focus:
pip install polars duckdb pyarrow feast mlflow questdb clickhouse-driver psycopg2-binary opentelemetry-sdk prometheus-client sentry-sdk

# Tier B neuromorphic:
pip install norse-torch bindsnet brian2

# Tier B NLP/transformers:
pip install spacy transformers sentence-transformers openai-whisper TTS

# Tier B multi-agent:
pip install autogen-agentchat crewai semantic-kernel

# Tier C distributed/federated (future only):
pip install ray[default] ray[rllib] flwr fedml temporalio dagster
